interface:
  display_name: "UI 方向实验室"
  short_description: "读取项目风格后生成多张独立 UI 参考图"
  default_prompt: "使用 $ui-direction-lab，先读取当前项目风格和我的设计倾向，QA 对齐后生成多张独立 UI 参考图。"

policy:
  allow_implicit_invocation: true
